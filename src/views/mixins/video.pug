mixin video(content)
     div
                h4
                   a(href=`/videos/${content.id}`)=content.title
                ul 
                    li #{content.meta.rating} / 5
                    li #{content.comments} comments.
                    li Posted #{content.createdAt}
                    li #{content.meta.views} views.